# 데이터 처리 및 DB 연결
pandas==2.0.3
sqlalchemy==2.0.21
pymysql==1.1.0
cx_Oracle==8.3.0
psycopg2-binary==2.9.9

# Elasticsearch 연동
elasticsearch==8.10.0

# PySpark (Spark 환경에 이미 설치되어 있다면 생략 가능하지만, 로컬 개발용으로 포함)
pyspark==3.4.1

# 유틸리티
pyyaml==6.0.1
cryptography==41.0.4